# Fixture: every reference resolves.
# Uses SAVE_GCODE_STATE (ALLOWLIST), G28/G90/G0 (G-code prefix range),
# and CG28_OK (defined in this file).

[gcode_macro CG28_OK]
gcode:
    G28

[gcode_macro PARK_OK]
description: park toolhead
gcode:
    SAVE_GCODE_STATE NAME=PARK
    G90
    G0 X100 Y0 F6000
    CG28_OK
    RESTORE_GCODE_STATE NAME=PARK
