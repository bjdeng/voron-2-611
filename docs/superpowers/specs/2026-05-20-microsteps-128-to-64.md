# Microsteps 128 → 64 on X/Y/Z mainboard steppers — empirical test

**Status:** Approved 2026-05-20. Implementation pending.
**Closes:** [#24](https://github.com/bjdeng/voron-2-611/issues/24)

## Background

All 6 mainboard steppers (X, Y, Z, Z1, Z2, Z3) currently run at `microsteps: 128` with `interpolate: False` and `stealthchop_threshold: 0` (always spreadCycle). This config was adopted from third-party forum advice with the stated goal "quiet without losing steps" — never empirically validated on this machine.

Two facts make the current config questionable:

1. **Klipper docs say 64 is acoustically equivalent to 128 with `interpolate: False`.** From `vendor/klipper/docs/TMC_Drivers.md:106-109`: "Typically, a microstep setting of 64 or 128 will have similar audible noise as interpolation, and do so without introducing a systemic positional error." The systemic error described in the same section only applies when `interpolate: True` — irrelevant here.

2. **MCU step-rate budget is tight at 128.** LPC1769 USB-limited to ~628k steps/sec per `vendor/klipper/docs/Benchmarks.md`. At `max_velocity = 450 mm/s`, CoreXY moves drive ~576k steps/sec across both X/Y motors at 128 microsteps — **~92% of MCU USB budget**. Currently zero `step_compress` warnings in klippy.log, so we're not hitting it, but margin is thin. Dropping to 64 microsteps would halve that to ~46% utilization, giving real headroom for input shaper scheduling and MMU activity spikes.

## Goal

Confirm whether 64 microsteps delivers equivalent noise to 128 on this specific machine (Voron 2.4, 350 mm, 5 USB MCUs, ERCF v2). If yes, ship the change and recover ~50% MCU step budget. If no, document the empirical result and keep 128.

## Non-goals

- **stealthChop testing.** Community consensus and Klipper's own warnings about mid-move mode switching make stealthChop on a high-accel CoreXY (10k mm/s²) X/Y a known risk. If 64sp doesn't satisfy quietness, stealthChop on Z motors (only) is a separate follow-up — out of scope here.
- **Lower microsteps (16, 32).** Klipper docs don't suggest these produce better acoustics; they may reintroduce stepper resonance noise that 64+ smooths out. Test scope stays 128 → 64; if 64 underperforms, that's the moment to discuss going lower.
- **MMU steppers** (on EASY-BRD / SAMD21G18A). Different MCU, different step rate budget, and changes would invalidate MMU calibration. Out of scope.
- **Extruder** (on EBB / RP2040). Different MCU with much higher step rate headroom; extruder doesn't move at MCU-stressing velocities. Out of scope.

## Change

Six lines in `config/motion.cfg`:
- `[stepper_x] microsteps: 128 → 64`
- `[stepper_y] microsteps: 128 → 64`
- `[stepper_z] microsteps: 128 → 64`
- `[stepper_z1] microsteps: 128 → 64`
- `[stepper_z2] microsteps: 128 → 64`
- `[stepper_z3] microsteps: 128 → 64`

No other config changes. `rotation_distance`, `interpolate: False`, `stealthchop_threshold: 0`, `run_current`, `sense_resistor` all stay. Klipper computes step pulses as `(distance / rotation_distance) × full_steps × microsteps` automatically — no other tuning is invalidated by a microstep change.

**Restart impact:** `RESTART` (Python-side stepper config + a re-emit of TMC SPI/UART register writes; no MCU pin or kinematics change).

## Test sequence (~30 min total)

**Phase 0 — Baseline capture (before any change):**
1. Confirm printer is homed + QGL'd
2. Run `TEST_SPEED` macro **twice**. Note: subjective noise rating (1-5 scale), any abnormal sounds (clunks, screeches, missed-step clicks), final position diff reported by the macro
3. `ssh pi@mainsailos.local "grep -c step_compress ~/printer_data/logs/klippy.log"` → record baseline (expected: 0)

**Phase 1 — Apply change:**
1. Branch → edit motion.cfg → local CI gates (`make refcheck && make test-py`)
2. PR + CI green → squash-merge → `/deploy-to-pi`
3. Klipper restart picks up new microsteps

**Phase 2 — Post-change verification:**
1. Home + QGL after firmware restart (clears homing state)
2. Run `TEST_SPEED` **twice**. Same observations as Phase 0
3. Quick test print: small benchy or Voron cube (~30 min). Visually inspect for surface artifacts vs typical prints
4. Post-print: `grep -c step_compress ~/printer_data/logs/klippy.log` → confirm zero

## Success criteria (ALL must pass for the change to stay)

| Criterion | Pass threshold |
|---|---|
| TEST_SPEED skip check | Position diff < 0.1 mm (the macro's own default) |
| Subjective noise | ≤ baseline rating (equal or quieter) |
| Test print surface | No new ringing, ghosting, or layer artifacts vs prior prints |
| MCU step rate | Zero `step_compress` warnings in klippy.log post-change |

## Rollback

Single revert PR restores 128 microsteps in all 6 sections. ~10 min total (PR + CI + deploy + restart). No printer state to clean up — microsteps don't persist anywhere outside motion.cfg.

## Outcome documentation

When closing issue #24:
- **Success path:** Comment with baseline vs post-change measurements, link to merged PR. Update CLAUDE.md "Known quirks" to remove the "Microsteps 128 (atypically high)" caveat or rephrase as "Microsteps 64, empirically validated 2026-05-20".
- **Failure path:** Comment with the specific failed criterion + measurement data. Revert PR is the immediate response. Keep CLAUDE.md's "atypically high" caveat but add the test evidence that 128 IS the right choice for this machine.

## References

- `vendor/klipper/docs/TMC_Drivers.md:90-115` — interpolation and microstep noise discussion
- `vendor/klipper/docs/Benchmarks.md` — LPC1769 step rate benchmarks (~628k steps/sec USB-limited)
- `config/motion.cfg:35,48,59,72,88,98,111,115,128,132,145,149` — current microsteps + interpolate settings
- `config/macros/test_speed.cfg` — TEST_SPEED macro definition
- CLAUDE.md "Known quirks" — current rationale for keeping 128
